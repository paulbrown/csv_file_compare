{
    "name": "Python Dev",
    "build":{
      "dockerfile": "Dockerfile"
    },
    "customizations": {
      "vscode": {
        "settings": {},
        "extensions": [
          "ms-python.python"
        ]
      }
    },
    "postStartCommand": "pip install --upgrade pip && pip install -r requirements.txt"
  }
